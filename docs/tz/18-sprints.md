> Раздел ТЗ horeca-shop. Версия, оглавление, история изменений и карта «спринт → разделы» — [TZ-horeca-shop.md](../../TZ-horeca-shop.md).

# 18. План спринтов

Порядок работ — два параллельных трека. **Бэкенд** не зависит от дизайна. **Вёрстка** витрины начинается только после утверждения соответствующих этапов макетов. Длительность зависит от скорости утверждения макетов и от поставщика, поэтому в днях не фиксируется.

**Трек «Дизайн»** (заказчик + Claude Design, по `DESIGN-BRIEF-horeca-shop.md`):
- **Д1** — название, логотип, референсы;
- **Д2** — токены, заглушка, состояния карточки товара;
- **Д3** — листинг и страница товара;
- **Д4** — шапка, поиск, главная;
- **Д5** — корзина, оформление, модалки;
- **Д6** — B2B и личный кабинет;
- **Д7** — служебные экраны и письма.

**Спринт 0 — Подготовка.** Выполнен 16.09.2026.
- Git-репозиторий создан; удалённый репозиторий на GitHub создаёт заказчик.
- Вопросы поставщику отправлены (§20).
- Портативные PHP 8.4.25, Composer 2.10.3 и MariaDB 11.8.9 по §17.0; архивы сверены с официальными контрольными суммами SHA-256.
- README с разделом «Локальная разработка».
- *DoD:*
  - `php -v` показывает 8.4 с модулями из §17.0;
  - `composer diagnose` подтверждает HTTPS-доступ к packagist;
  - MariaDB запускается и останавливается скриптами;
  - PHP через PDO подключается к базам `horeca` и `horeca_test`, кириллица сохраняется без искажений.

**Спринт 1 — Скелет.** Выполнен 16.09.2026, ветка `feature/sprint-1-skeleton`.
- Laravel 13, MariaDB, Redis с запасным режимом `database`.
- Filament 5 на `/manage` с 2FA; роли на enum и Policies.
- Все миграции §5, модели со связями, фабрики, `DemoSeeder`, `ProductionSeeder`.
- Pint, Pest, `.env.example`, README.
- Tailwind 4 с токенами §9 и минимальный layout без дизайна витрины.
- *DoD:* `php artisan test` зелёный; администратор входит в `/manage` с 2FA; демо-данные в базе.
- *Итог:*
  - 152 теста, Pint и `npm run build` проходят;
  - гость перенаправляется на вход, клиент получает 403, сотрудник без 2FA отправляется на её настройку, с 2FA — попадает в админку;
  - `migrate:fresh --seed` создаёт справочники и демо-каталог.
- *Сделано сверх списка, потому что понадобилось уже сейчас:* `Money` и `Percent` с кастами, `GenerateOrderNumber` (номера заказов для демо-данных), `RecalculateCategoryCounts`, `CatalogQuery` и главная со списком категорий, русские `lang/ru` для валидации и входа.

**Спринт 2 — Импорт XML.** Выполнен 18.09.2026, ветка `feature/sprint-2-import`.
- Контракт, DTO, `Cp1251XmlReader`, адаптеры каталога и остатков, условный GET.
- Staging, пороги, `CategorySync`, `BrandSync`, `ProductUpserter`, `StockUpserter`, `AvailabilityCalculator`.
- Расчёт розничной цены, `search_text`.
- Очередь `imports` с отдельным соединением, команда `supplier:import`.
- Filament-ресурсы: поставщики, профили, прогоны, соответствия, склады.
- Итоги дня и тревоги в Telegram, фикстуры и тесты §16.
- *DoD:*
  - оба реальных файла заливаются: около 15 000 товаров, 324 категории в неактивном зеркале, статусы наличия совпадают с разбором данных с поправкой на свежесть файлов;
  - повторный прогон даёт `skipped` или `unchanged`;
  - ручные правки не затираются;
  - битый файл не меняет каталог.
- *Итог:*
  - 267 тестов, Pint и `npm run build` проходят;
  - реальные выгрузки 17–18.09.2026: 15 007 товаров, 324 категории в неактивном зеркале (25 корней), 64 бренда, у всех товаров есть категория и бренд, 1 024 товара с ценой по запросу; 20 складов, 5 943 товара с остатками, 4 606 позиций только в остатках; в наличии 1 812 товаров (12 %) — как в разборе данных;
  - прогон каталога 7–27 с, остатков 4–8 с, пик памяти 58 МБ; повторный прогон того же файла — `skipped`, с `--force` — 15 017 `unchanged`.

**Спринт 3 — Витрина каталога** (после Д2–Д4). Закрыт 21.09.2026, ветка `feature/sprint-3-catalog`, 501 тест; стейджинг — `test.gastrosnab.ru` в контейнерах Docker (§17.1, §17.2).
- Библиотека Blade-компонентов и локальная страница-стайлгайд для сверки с макетами.
- Главная, `/catalog`, листинг с фильтрами, сортировкой и режимами вида, карточка товара, бренды, поиск.
- Сравнение моделей (§8.5).
- Хлебные крошки, микроразметка, мобильная версия, скелетоны, 404.
- Выкладка на стейджинг — сделана 22.09.2026: `test.gastrosnab.ru` в контейнерах Docker (§17). Lighthouse mobile на стейджинге: категория 100 / 100 / 100 (LCP 1,2 с), карточка 96 / 100 / 100 (LCP 2,2 с), ответ сервера 155–225 мс. LCP карточки выше цели 2,0 с — разбор в спринте 6 (§14). Проверка на реальных телефонах — за заказчиком.
- *DoD:* Lighthouse mobile ≥ 90 / ≥ 95 на категории и карточке; вёрстка совпадает с утверждёнными макетами.
- *Стайлгайд, 21.09.2026:* страница `/styleguide` (только при `APP_ENV=local`) повторяет экран 9 макета и показывает все компоненты витрины во всех состояниях; по сверке кнопки переведены на Manrope 500 14 px, статусы наличия получили форму и маркеры из макета, добавлены поле с ошибкой, счётчик и тумблер.
- *Каркас витрины, 21.09.2026:* служебная полоса с контактами и включёнными страницами, липкая шапка с поиском, ряд корневых разделов с «Ещё», меню-бургер на планшете и телефоне, лента разделов на телефоне, футер с разделами, поиском по артикулу и реквизитами — по экранам 5, 10 и 14. Корзина, вход и избранное появятся в шапке со своими спринтами.
- *Листинг, 21.09.2026:* компонент Livewire `CategoryListing` — фильтры без перезагрузки с состоянием в адресе, счётчики при остальных фильтрах, чипы, «Показать ещё» и номера страниц, плитка и список с запоминанием в cookie, шторка фильтров ниже 1280 на `popover`, горизонтальные карточки на телефоне; без скриптов — те же ссылки и GET-форма. Фильтры по характеристикам появятся с данными API (спринт 7).
- *Мгновенный поиск, 21.09.2026:* выдача под полем в шапке (Livewire `InstantSearch`, 250 мс, от двух символов, 6 товаров с ценой и наличием, 3 раздела, «Показать все N результатов»), стрелками по строкам, Esc закрывает; поле «Знаю артикул» на главной.
- *Страница поиска, 21.09.2026:* Livewire `SearchListing` по экрану 8 — точное совпадение по артикулу отдельной карточкой, «Уточнить:» по разделам, фильтры листинга, строки вместо сетки, честная пустая выдача с крупными разделами; LIKE по `search_text` один раз на страницу, счётчики и фасеты — по найденным id (страница 0,4–0,5 с локально, как листинг).
- *Главная, 21.09.2026:* по экрану 4 — плитки корневых разделов «На главной» с числом позиций, в наличии и крупнейшими подразделами (кэш каталога), «Знаю артикул» на месте панели подбора, ленты «В наличии», «Готово к отгрузке: <склад>» (от `catalog.local_strip_min_products`), «Часто заказывают» и «Новинки» — пустые не показываются. Подборки «Соберём кухню под задачу» — со спринтом 6, вставка списка артикулов — со спринтом 5.
- *Карточка товара, 21.09.2026:* по экрану 3 (случаи 4a и 4b) и экрану 14 — галерея или честная заглушка «Фото уточняется у производителя», липкая панель покупки справа (цена, счётчик, остатки по складам со сроками, гарантия, «Проверьте перед монтажом» — габариты, вес, главные характеристики), «Характеристики / Описание / Доставка и оплата / Гарантия» вкладками с 768 px и аккордеонами на телефоне, пустые разделы не выводятся, короткая таблица не растягивается; «Часто берут вместе», «Похожие товары», липкая полоса покупки ниже 1024 px, микроразметка `Product` + `Offer` (без цены — без `Offer`). Без скриптов все разделы идут подряд. Корзина под кнопками — спринт 4, «Купить в 1 клик» и избранное — со спринтами 4–5.
- *Бренды, 21.09.2026:* `/brands` — названия по буквам с числом моделей на витрине (кэш каталога); `/brands/{slug}` — Livewire `BrandListing` с фильтрами, сортировкой и видом листинга, без фильтра по бренду, зато с «Разделы:» (8 разделов, где у бренда больше всего моделей); выключенный бренд — 404. На главной — 24 ведущих бренда и «Все N брендов» (§8.1, п. 5), в футере — «Все бренды», в карточке товара бренд ведёт на свою страницу. Общие части листингов вынесены в компоненты (`filter-bar`, `filter-chips`, `product-grid`, `section-links`) и трейты (`ChoosesView`, `RefinesBySection`). Отдельного экрана в макетах нет — страницы собраны из компонентов экранов 2, 8 и 9.
- *Сравнение, 21.09.2026:* по экрану 13 — «Сравнить» на карточке листинга, в строке поиска и таблице списка, «К сравнению» на странице товара; до 4 моделей, у гостя в сессии, у вошедшего — в `compare_items`, при входе списки объединяются. Формы работают без скриптов (возврат с уведомлением), со скриптами — без перезагрузки: кнопка меняется на всех карточках товара, «Сравнение · N» в шапке, уведомление внизу. `/compare` — таблица с закреплённой колонкой параметров, «Критично для монтажа» (габариты, масса, главные характеристики), затем характеристики; по умолчанию только различия, совпадающее — сноской, «Все N строк»; снятая модель остаётся со статусом, скрытая выпадает; `noindex`. На реальных данных пока сравниваются бренд и модель — характеристики придут с API (спринт 7).
- *Редиректы, страницы и ошибки, 21.09.2026:* `Route::fallback` — редирект из `redirects` (301 или указанный код, счётчик `hits`, строка запроса сохраняется), затем включённая страница по slug (Markdown), затем 404. Таблица редиректов проверяется при любой 404 — так работают старые адреса товаров, разделов и брендов. 404 — в каркасе витрины с поиском и разделами, `noindex`; 403, 419, 429, 500, 503 и общие 4xx/5xx — отдельный лёгкий шаблон без базы и Livewire, тексты по-русски. По экрану 11: кода ошибки на экране нет, у каждой страницы второй путь — каталог; пульсация листинга при загрузке — только если ответ дольше 300 мс.
- *Закрытие спринта, 21.09.2026:* Lighthouse mobile локально, без отладки (`php artisan serve`, без сжатия и OPcache): раздел «Нейтральное оборудование» (7 021 модель) — Performance 91, Accessibility 100, Best Practices 100, SEO 91; карточка товара — 98 / 100 / 100 / 91. По замерам поправлены контраст ссылок на сером фоне футера и порядок заголовков листинга на телефоне; добавлена разметка `Organization` на главной. LCP раздела 2,7 с при цели 2,0 с — на локальном сервере CSS идёт без сжатия (51 КБ вместо ~10 КБ) и первый ответ 0,4–0,5 с; перемерить на стейджинге. SEO 91 — нет `meta description` у разделов и товаров: шаблоны `MetaBuilder` — спринт 6. Выкладка на стейджинг ждёт домен и SSH.
- *Исправлено попутно:* слушатели событий регистрировались дважды (явно и автопоиском Laravel) — тревога о сбое импорта уходила в Telegram и на почту по два раза; теперь только автопоиск, тест проверяет одно сообщение.
- *Сделано заранее, 18.09.2026, ветка `feature/sprint-3-catalog`* — бэкенд, не зависящий от макетов: ресурсы Filament «Товары», «Категории», «Бренды» по §12 (замки ручных правок, «Вернуть значение поставщика», фильтры, массовые действия, выгрузка в XLSX, объединение брендов), 301-редиректы при смене адресов, дашборд с последними прогонами импорта и тем, что каталог ждёт от менеджера; `PriceResolver` (§7); выборки листинга с фильтрами, сортировками, фасетами брендов и цен (`CatalogQuery`, §8.2); поиск (`QueryNormalizer`, `DatabaseSearchEngine`, `ProductSearch`, §8.4) — на реальном каталоге 30–113 мс. 359 тестов.

**Спринт 4 — Корзина и заявка** (после Д5). Закрыт 22.09.2026, ветка `feature/sprint-4-cart`; сценарии 1, 7, 8 из §2 — сквозными тестами `tests/Feature/Scenarios`, путь покупателя и короткие заявки проверены в браузере.
- Серверная корзина и объединение, оформление, `idempotency_key`, антиспам.
- Номера заказов, «Спасибо», уведомления.
- Лиды и модалки, Filament-ресурсы заказов и лидов.
- *DoD:* сценарии 1, 7, 8 проходят тестами и руками.
- *Корзина, 22.09.2026, ветка `feature/sprint-4-cart`:* `AddToCart` фиксирует цену и суммирует количество (1–9999), цену по запросу, снятые и скрытые товары не кладёт; гостевая корзина — 30 дней за своей cookie, при входе сливается с корзиной клиента (`MergeGuestCart`); `CartReview` при каждом открытии пересчитывает цены с плашкой «Цена изменилась», подсвечивает позиции, которые больше нельзя купить, и держит оформление до их удаления, считает вес и путь до бесплатной доставки. «В корзину» — обычная форма на карточке, в строке поиска, таблице списка, панели и полосе покупки, в сравнении; со скриптами без перезагрузки. В шапке — корзина с числом позиций и суммой (экран 5). Страница `/cart` (экран 6, Livewire `CartPage`): ± и ручной ввод, 0 и «×» удаляют с «Вернуть» на 10 секунд, очистка; без скриптов — те же формы.
- *Оформление, 22.09.2026:* `/checkout` по экрану 12 — контакты с маской телефона, «Я представляю юрлицо» с ИНН (проверка контрольных цифр), получение карточками (самовывоз, ТК с городом и выбором ТК, курьер с адресом), оплата, комментарий, липкая сводка с согласием и списком ошибок; поля раскрываются без скриптов. `PlaceOrder` одной транзакцией создаёт заказ со снимком позиций, номер `HR-ДДММГГ-NNNN`, запись в журнал статусов и очищает корзину; повтор с тем же `idempotency_key` ведёт к созданному заказу; событие `OrderCreated`. Антиспам — ловушка и метка времени (быстрее 3 с — отказ), 10 заявок в час с IP и 3 на телефон. «Спасибо» — только своей сессии. Опт — для клиента одобренной компании, UTM-метки — в заказ.
- *Админка продаж, 22.09.2026:* «Заявки» — вкладки по статусам с числом и цветными бейджами, бланк с `tel:`, составом таблицей и текстом для копирования, историей статусов; «Сменить статус» (отмена — только с причиной, «Оплачена» ставит дату оплаты), «Позвонили», счёт PDF на приватном диске, мягкое удаление и возврат — только администратором. «Лиды» — статус меняется прямо в таблице. Дашборд — новые заявки, заявки за день и неделю с графиком, новые лиды, компании на проверке.
- *Уведомления, 22.09.2026:* новая заявка — Telegram (номер, тип, сумма, до 10 позиций, получение, ссылка на бланк; имя и телефон — только с `notify.telegram_include_contacts`), письмо менеджерам с таблицей, подтверждение клиенту, если он оставил почту; смена статуса — письмо клиенту о подтверждении, счёте, оплате, отгрузке, выполнении и отмене с причиной. Лид — только Telegram. Всё через очередь `default`.
- *Лиды, 22.09.2026:* `POST /leads` — «Купить в 1 клик», «Запросить цену», «Уточнить срок», «Подобрать аналог» в окнах на нативном `popover` (без скриптов открываются и отправляются; со скриптами — без перезагрузки, ошибки у полей), «Найдём за вас» на пустой выдаче поиска. «Запросить цену» в листингах — одно окно на страницу, товар подставляет кнопка. Антиспам как у оформления, 10 лидов в час с IP.

**Спринт 5 — B2B** (после Д6).
- Регистрация оптовика, модерация, ценовые группы, `PriceResolver` с ограничениями, две цены.
- ЛК: сводка, заявки, повтор, компания, избранное.
- Заказ списком, прайс XLSX, middleware.
- *DoD:* сценарии 3–5 проходят тестами; неодобренная компания не видит опт.
- *Личный кабинет, 23.09.2026, ветка `feature/sprint-5-b2b`:* `/account` по экрану 7 в объёме MVP — оптовые цены, контакты менеджера, быстрые действия, последние 5 заявок; «Заявки» по 20 с оплатой и отгрузкой отдельными колонками; заявка с позициями, итогом, получением, покупателем и историей статусов; «Повторить заказ» (`RepeatOrder`) кладёт в корзину по текущим ценам, снятые, скрытые, удалённые и с ценой по запросу — списком «Не добавлены» над корзиной; счёт PDF с приватного диска после проверки `OrderPolicy`; «Компания» — реквизиты организации, банка и контакт (`UpdateCompanyDetails`). После входа — в кабинет, в меню шапки — «Личный кабинет» и «Мои заявки».

**Спринт 6 — Контент и SEO** (Д7).
- Страницы CMS, страница настроек, `MetaBuilder`, sitemap, robots, редиректы.
- Метрика с целями и cookie-баннер, сопутствующие товары, подборки на главной, пересчёт популярности, письма.
- *DoD:* обязательные страницы наполнены текстами заказчика; sitemap валиден; цели Метрики срабатывают.

**Спринт 7 — API Росхолода** (когда поставщик даст документацию и доступ).
- `RosholodApiSource`, авторизация.
- Фото через medialibrary.
- Характеристики: `AttributeValueParser`, новые характеристики создаются нефильтруемыми.
- Числовые остатки, дилерская цена, когда появится.
- Отчёт dry-run по сопоставлению GUID до переключения; перевод XML-профилей в резерв.
- *DoD:*
  - не меньше 99% товаров API сопоставлены с существующими по GUID;
  - фото и характеристики на витрине;
  - XML-профили выключены, но запускаются вручную.

**Спринт 8 — Прод.**
- Развёртывание по §17 на `gastrosnab.ru`, HTTPS, 301 с `гастроснаб.рф` и `www`.
- Бэкапы с внешней копией (не в бакете S3 сайта, §17.9) и проверкой восстановления, почта `shop@gastrosnab.ru`.
- Мониторинг ошибок в Telegram, нагрузочная проверка каталога, финальный прогон §19.

Запуск возможен на XML-источниках: спринт 7 выполняется, когда поставщик будет готов, до или после спринта 8.
