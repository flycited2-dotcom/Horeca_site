> Раздел ТЗ horeca-shop. Версия, оглавление, история изменений и карта «спринт → разделы» — [TZ-horeca-shop.md](../../TZ-horeca-shop.md).

# 11. Личный кабинет и B2B

**Регистрация оптовика** (`/wholesale`).
- **Лендинг:** как подключиться (заявка → проверка → оптовые цены) и выгоды — только подтверждённые заказчиком (оптовые цены, персональный менеджер, работа по безналу, отсрочка — если есть). Выгоды — текст страницы «Оптовикам» (`optovikam`), который ведёт администратор: пока страница выключена, блока нет. Адрес `/optovikam` отдаёт 301 на `/wholesale`, ссылки «Оптовикам» в шапке и футере ведут туда же и видны всегда.
- **Форма:** ИНН, юр. название, сегмент, контактное лицо, телефон, e-mail, город, пароль, комментарий.
- **Результат:** создаются `user` (`customer`) и `company` (`pending`), пользователь связан с компанией. Вход работает сразу, цены — розничные. Вошедший клиент без компании подаёт заявку без пароля — компания привязывается к его кабинету. Одна заявка на кабинет; почта и телефон нового кабинета не должны принадлежать другому. После отправки `/wholesale` показывает статус заявки (макет, экран 15d).
- Автоподстановка реквизитов по ИНН — вне MVP.

**Модерация.**
- Новая заявка → уведомление менеджерам (§13).
- Менеджер назначает ценовую группу и ставит `approved` → письмо клиенту «Оптовые цены открыты».

**Разделы ЛК:**
- **Сводка:** статус компании, подключены ли оптовые цены, последние 5 заявок, контакты менеджера (телефоны, почта и часы работы из настроек — персонального менеджера в MVP нет).
- **Заявки:**
  - список со статусами: оплата и отгрузка — две колонки (макет, экран 7), выводятся из статуса заявки, даты оплаты и счёта;
  - детальная страница с позициями и историей статусов — отправка и статусы, о которых клиенту пишут письма (§13), с комментарием менеджера;
  - «Повторить заказ» (`RepeatOrder`): текущие цены; позиции со статусом «Снят с производства», с ценой по запросу или удалённые — в отдельном списке «Не добавлены»;
  - скачивание счёта (PDF), когда менеджер его прикрепит.
- **Компания:** редактирование реквизитов. Смена ИНН или юр. названия возвращает одобренную или отклонённую компанию в `pending`, до повторного одобрения цены розничные; менеджеры получают уведомление о повторной проверке (§13). Заблокированная компания ИНН и название не меняет.
- **Заказ списком:**
  - строки `артикул;количество` или `артикул<TAB>количество`, либо XLSX до 1 МБ и до 500 строк (чтение через OpenSpout);
  - сопоставление по сжатому артикулу, затем по коду 1С;
  - превью построчно: найден / не найден / несколько совпадений (выбор товара) / цена по запросу (не добавляется, есть кнопка «Запросить цену на эти позиции» — один лид) / снят с производства;
  - «Добавить в корзину».
- **Прайс:** скачивание XLSX (§7).
- **Избранное.**

**Middleware `EnsureWholesaleApproved`** закрывает «Заказ списком» и «Прайс».
